<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath />
    </parent>

    <groupId>com.shoestore</groupId>
    <artifactId>shoe-store-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>Shoe Store Backend</name>
    <description>
        Production-oriented modular monolith backend for a shoe store.
    </description>

    <properties>
        <java.version>21</java.version>

        <maven.compiler.release>21</maven.compiler.release>
        <maven.compiler.parameters>true</maven.compiler.parameters>
        <maven.compiler.showWarnings>true</maven.compiler.showWarnings>
        <maven.compiler.failOnWarning>true</maven.compiler.failOnWarning>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <maven-enforcer-plugin.version>3.6.3</maven-enforcer-plugin.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc</artifactId>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>

        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-mysql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-mysql</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>${maven-enforcer-plugin.version}</version>

                <executions>
                    <execution>
                        <id>enforce-build-baseline</id>

                        <goals>
                            <goal>enforce</goal>
                        </goals>

                        <configuration>
                            <fail>true</fail>

                            <rules>
                                <requireMavenVersion>
                                    <version>[3.9.16]</version>
                                    <message>
                                        This project requires Apache Maven 3.9.16.
                                        Use the committed Maven Wrapper: mvnw.cmd
                                    </message>
                                </requireMavenVersion>

                                <requireJavaVersion>
                                    <version>[21.0.6,21.0.7)</version>
                                    <display>true</display>
                                    <message>
                                        This project requires Oracle JDK 21.0.6.
                                    </message>
                                </requireJavaVersion>

                                <requireJavaVendor>
                                    <includes>
                                        <include>Oracle Corporation</include>
                                    </includes>
                                    <message>
                                        This project requires Oracle JDK according to the locked
                                        technology baseline.
                                    </message>
                                </requireJavaVendor>

                                <banDuplicatePomDependencyVersions>
                                    <message>
                                        Duplicate dependency declarations are not allowed.
                                    </message>
                                </banDuplicatePomDependencyVersions>

                                <banDynamicVersions>
                                    <message>
                                        Dynamic, LATEST, RELEASE, version-range and SNAPSHOT
                                        dependency versions are not allowed.
                                    </message>
                                </banDynamicVersions>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>

                <configuration>
                    <release>${maven.compiler.release}</release>
                    <parameters>${maven.compiler.parameters}</parameters>
                    <showWarnings>${maven.compiler.showWarnings}</showWarnings>
                    <failOnWarning>${maven.compiler.failOnWarning}</failOnWarning>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
